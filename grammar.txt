Order of priority: LOW to HIGH


expr    : KEYWORD:VAR IDENTIFIER EQ expr
		:	term ((PLUS|MINUS) term)*

term        : factor((MUL|DIV) factor)*

factor		: (PLUS|MINUS) factor

power		: super (POW factor)*

atom		: INT|FLOAT
			: LPAREN expr RPAREN

